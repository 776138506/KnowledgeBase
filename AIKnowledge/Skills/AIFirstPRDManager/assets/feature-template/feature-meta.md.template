# feature-meta.md - [功能名称]

> 功能核心元信息，定义功能的基本属性和范围
> 
**创建时间**: YYYY-MM-DD  
**创建者**: [姓名]  
**当前阶段**: Phase 0 (初始化)

---

## 基本信息

| 属性 | 值 |
|-----|---|
| **功能 ID** | F-[XXX] |
| **功能名称** | [中文名称] |
| **功能标识** | [kebab-case 标识，如 user-register] |
| **功能类型** | 核心功能 / 辅助功能 / 基础设施 |
| **优先级** | P0 (必须) / P1 (重要) / P2 (一般) / P3 (可选) |
| **状态** | 规划中 / 开发中 / 测试中 / 已完成 | 
| **预计工期** | {X} 天 |

## 功能描述

### 一句话描述
[用 1-2 句话概括这个功能做什么]

### 详细说明

[详细描述功能的背景、目标、价值]

#### 背景为什么需要这个功能？
- [原因 1]
- [原因 2]

#### 目标这个功能要达成什么目标？
- [目标 1]
- [目标 2]

#### 价值这个功能带来什么价值？
- 业务价值: [描述]
- 用户价值: [描述]
- 技术价值: [描述]

---

## 适用范围

### 目标用户

| 用户角色 | 描述 | 使用频率 |
|---------|------|---------|
| [角色 1] | [描述] | 高/中/低 |
| [角色 2] | [描述] | 高/中/低 |

### 使用场景

**主要场景（Must Have）**:
1. **[场景 1]**: [描述用户在什么情况下使用]
2. **[场景 2]**: [描述用户在什么情况下使用]
3. **[场景 3]**: [描述用户在什么情况下使用]

**次要场景（Nice to Have）**:
1. **[场景 4]**: [描述]
2. **[场景 5]**: [描述]

### 不做范围（Explicitly Out of Scope）

明确标注**不做什么**，避免 AI 臆造需求：

- ❌ [不做的事项 1]: [原因]
- ❌ [不做的事项 2]: [原因]
- ❌ [不做的事项 3]: [原因]

---

## 技术约束

### 必须遵循的约束

从 `docs/project-constraint.yaml` 继承的全局约束：

- ✅ 技术栈: Python 3.10 + FastAPI + MySQL
- ✅ 编码规范: PEP8, Ruff, Black
- ✅ 测试要求: 单元测试覆盖率 ≥80%
- ✅ 文档要求: 公共函数必须有 docstring

### 功能特定约束

本功能特有的技术约束：

| 约束项 | 约束值 | 原因 |
|-------|--------|------|
| [约束 1] | [值] | [原因] |
| [约束 2] | [值] | [原因] |
| [约束 3] | [值] | [原因] |

### 性能要求

| 指标 | 目标值 | 说明 |
|-----|--------|------|
| 响应时间 (P99) | < {X}ms | 99% 的请求响应时间 |
| 吞吐量 (QPS) | > {X} | 每秒处理请求数 |
| 并发支持 | > {X} 用户 | 同时在线用户数 |
| 数据量 | < {X} 条 | 预期数据规模 |

---

## 依赖关系

### 上游依赖（此功能依赖的其他功能/系统）

| 依赖 ID | 依赖名称 | 依赖类型 | 状态 |
|---------|---------|---------|------|
| [ID] | [名称] | 功能依赖/API 依赖/数据依赖 | 已就绪/待开发 |

### 下游依赖（依赖此功能的其他功能）

| 依赖者 ID | 依赖者名称 | 依赖方式 | 计划时间 |
|-----------|-----------|---------|---------|
| [ID] | [名称] | 调用 API / 使用数据 / 触发事件 | YYYY-MM-DD |

### 外部依赖（第三方服务/系统）

| 服务名称 | 用途 | SLA 要求 | 备选方案 |
|---------|------|---------|---------|
| [服务名] | [用途] | 可用性 ≥99.9% | [备选] |

---

## 成功标准

### 功能完整性验收

- [ ] 所有"主要场景"已实现并通过测试
- [ ] 所有"不做范围"未被实现（确认无越界）
- [ ] 性能指标达标
- [ ] 错误处理完善（所有异常情况有友好提示）
- [ ] 安全性符合要求（无 SQL 注入、XSS 等）

### 质量指标

| 指标 | 目标值 | 实际值 | 状态 |
|-----|--------|--------|------|
| 单元测试覆盖率 | ≥80% | { }% | ☐ |
| 集成测试通过率 | 100% | { }% | ☐ |
| E2E 测试通过率 | 100% | { }% | ☐ |
| 代码重复率 | <5% | { }% | ☐ |
| 圈复杂度平均 | <10 | { } | ☐ |
| Lint 错误数 | 0 | { } | ☐ |

### 用户验收标准

- [ ] 用户能够独立完成所有主要场景的操作
- [ ] 操作流程直观易懂（无需培训）
- [ ] 错误提示清晰友好
- [ ] 响应速度满足预期
- [ ] 整体体验流畅无卡顿

---

## 风险评估

### 技术风险

| 风险 ID | 风险描述 | 可能性 | 影响程度 | 缓解措施 | 负责人 |
|---------|---------|-------|---------|---------|--------|
| R-[001] | [描述] | 高/中/低 | 高/中/低 | [措施] | [姓名] |

### 业务风险

| 风险 ID | 风险描述 | 可能性 | 影响程度 | 缓解措施 | 负责人 |
|---------|---------|-------|---------|---------|--------|
| R-[002] | [描述] | 高/中/低 | 高/中/低 | [措施] | [姓名] |

---

## 追溯信息

### 关联文档

| 文档 | 路径 | 状态 |
|------|------|------|
| 需求设计 | requirement-design.md | 待编写 |
| 任务清单 | task-checklist.md | 待编写 |
| 用例图 | specs/01-usecase.md | 待编写 |
| 活动图 | specs/02-activity.md | 待编写 |
| 组件图 | specs/03-component.md | 待编写 |
| PERT 图 | specs/04-pert.md | 待编写 |
| 时序图 | specs/05-sequence.md | 待编写 |
| ER 图 | specs/06-er.md | 待编写 |
| 状态机图 | specs/07-state-machine.md | 待编写 |
| 业务规则表 | specs/08-business-rules.md | 待编写 |
| Gherkin 场景 | specs/09-gherkin.md | 待编写 |

### 参与人员

| 角色 | 姓名 | 职责 |
|------|------|------|
| 产品负责人 | [姓名] | 需求确认、验收 |
| 技术负责人 | [姓名] | 架构设计、技术决策 |
| 开发人员 | [姓名] | 编码实现、单元测试 |
| 测试人员 | [姓名] | 集成测试、E2E 测试 |

---

## 版本历史

| 版本 | 日期 | 作者 | 变更内容 |
|-----|------|------|---------|
| 0.1.0 | YYYY-MM-DD | [姓名] | 初始版本 |

---

**最后更新**: YYYY-MM-DD  
**下次更新**: 完成每个阶段后更新状态
